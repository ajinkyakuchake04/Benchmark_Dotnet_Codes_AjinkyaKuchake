using System;
using System.Collections;//.Collections removed (Non generic)
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace CollectionsDemo
{
    internal class Program
    {
        static void Main(string[] args)
        {
            ArrayList list = new ArrayList();
            //list.Add(10);
            //list.Add('e');
            //list.Add("Ajinkya");
            //list.Add(DateTime.Now);
            //list.Reverse();
            //list.Insert(2,"Kuchake"); //o index based
            //sorting can be done if elemenst of same type
            //list.Remove(10); //removes first occurence of element
            //list.RemoveAt(2); //removes element at given index

            //foreach (object o in list)
            //{
            //    Console.WriteLine(o);
            //}
        


            Console.ReadKey();
        }
    }
}
